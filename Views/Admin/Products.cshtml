
@{
    ViewBag.Title = "Products";
    Layout = "~/Layouts/AdminLayout.cshtml";
}

<div class="container-fluid bg-white rounded-4 h-100">
    <div class="row h-100">
        <div class="col-6 p-4 h-100 overflow-y-auto">
            <div class="h2 mb-3">Onay Bekleyen Ürünler</div>
            <table class="table table-dark table-hover rounded-3 overflow-hidden align-middle">
                <thead>
                    <tr>
                        <th scope="col" class="text-center">#</th>
                        <th scope="col" width="350px">Ürün</th>
                        <th scope="col" class="text-center">Satıcı Id</th>
                        <th scope="col">Fiyat</th>
                        <th scope="col">İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in ViewBag.OnaysizUrunler)
                     {
                        <tr>
                            <th scope="row" class="fs-5 text-center">@product.Id</th>
                            <td class="fs-5">@product.Name</td>
                            <td class="fs-5 text-center">@product.SellerID</td>
                            <td class="fs-5">@product.Price</td>
                            <td>
                                <a href="/Admin/ProductApprove/@product.Id" class="btn btn-success p-1 me-2">Onayla</a>
                                <a href="/Admin/DeleteProduct/@product.Id" class="btn btn-outline-danger p-1">Reddet</a>
                            </td>
                        </tr>
                     }
                </tbody>
            </table>
        </div>

        <div class="col-6 p-4 h-100 overflow-y-auto">
            <div class="h2 mb-3">Onaylı Ürünler</div>
            <table class="table table-dark table-hover rounded-3 overflow-hidden align-middle">
                <thead>
                    <tr>
                        <th scope="col" class="text-center">#</th>
                        <th scope="col" width="400px">Ürün</th>
                        <th scope="col" class="text-center">Satıcı Id</th>
                        <th scope="col">Fiyat</th>
                        <th scope="col">İşlem</th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var product in ViewBag.OnayliUrunler)
                     {
                        <tr>
                            <th scope="row" class="fs-5 text-center">@product.Id</th>
                            <td class="fs-5">@product.Name</td>
                            <td class="fs-5 text-center">@product.SellerID</td>
                            <td class="fs-5">@product.Price</td>
                            <td><a href="/Admin/DeleteProduct/@product.Id" class="btn btn-outline-danger py-1 px-3">Sil</a></td>
                            
                        </tr>
                     }

                </tbody>
            </table>
        </div>

    </div>
</div>
